using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResetHope : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GameData.ResetHope();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
